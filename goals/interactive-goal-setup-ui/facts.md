- The setup-goal skill markdown no longer tells agents to ask interview questions one at a time.
- The setup-goal skill tells agents to prepare a complete question bundle for the user when a goal needs interview input.
- The setup-goal skill tells agents to use the new Plannotator goal setup UI for the bundled interview before producing `facts.md`.
- The setup-goal skill tells agents to run the goal setup UI command as a monitored bash process and continuously wait for its output.
- The setup-goal skill tells agents to be patient while the user completes the UI, even when the user takes minutes or hours.
- The setup-goal skill keeps clear instructions for turning accepted facts into a high-quality ordered implementation plan.
- The goal setup UI reuses the existing Plannotator shell, theme tokens, app header, central scroll area, and review-gate decision model.
- The goal setup UI presents all interview questions in one interactive session so the user can answer them quickly without multiple chat turns.
- The interview UI defaults to showing questions vertically so the user can scan, scroll, and answer without unnecessary open-close clicks.
- The interview UI provides quick navigation between questions, including a next-question action and keyboard shortcut.
- Each interview question has an agent-recommended answer that can be accepted as-is.
- Each interview question may have agent-recommended answer options when options fit the question.
- Question options support single-select, multi-select, custom freeform answers, and combining selected options with a custom answer.
- Each interview answer is editable by the user before submission.
- Recommended placeholder text is easy to clear when the user focuses or starts typing a custom answer.
- Expanded question details use motion-based interactions only where they reduce friction or clarify editing.
- The Q&A UI may use accordion-like or morphing patterns for secondary detail, but the primary workflow remains fast vertical answering.
- The facts acceptance UI renders facts as a simple vertical list for fast reading.
- Each fact has right-side quick actions for accept, edit, remove, comment, and automated-verification selection.
- Fact comments reuse Plannotator's existing comment popover patterns where possible.
- A morphing popover is used for facts only if a fact exposes additional structured detail that benefits from expansion.
- The implementation adds or promotes shared UI primitives that feel shadcn-like while using Plannotator's existing design tokens.
- New shared primitives are organized in composable locations and avoid duplicating existing header, gate, popover, button, or review-shell behavior.
- Existing deeply embedded UI pieces are refactored into reusable primitives when the new goal setup UI needs them.
- The implementation uses `motion` for polished interactions where the designer determines it improves the user experience, honoring reduced-motion preferences.
- The implementation adds `lucide-react` icons where they improve button and control recognition.
- Submitting the interview returns structured question and answer data to the agent or writes an equivalent durable artifact for the agent to consume.
- The structured interview data model includes stable question ids, prompts, answer mode, recommended answers, option lists, selected option ids, custom answer text, user answer text, and completion state.
- Answer options in the data model can declare whether the user chooses one option, many options, or options plus custom input.
- Each fact can be accepted, edited, removed, or commented on before final approval.
- Facts accepted in an earlier iterative review pass are filtered out of later review passes unless the user explicitly asks to see accepted facts.
- The facts acceptance UI preserves accepted-fact state across iterative review passes.
- Each fact shows whether the agent recommends automated verification for it.
- The automated verification recommendation is preselected by the agent in the facts data model.
- The user can toggle automated verification selection for each fact.
- The facts data model preserves accepted fact text and automated verification selection after submission.
- The final `facts.md` remains readable as a flat fact sheet for humans and agents.
- Any structured facts metadata is stored in a predictable companion artifact or embedded format that the planning phase can consume without scraping UI state.
- A facts-diff view that shows changes between iterative fact review passes is treated as a bonus phase after the core Q&A and facts acceptance flow works.
- The plan-generation phase uses the accepted facts and automated verification selections to produce concrete verification steps.
- The existing markdown plan viewer remains available for normal plan, annotate, archive, and diff workflows.
- New server endpoints or modes are mirrored across the Bun server and Pi server when they become part of the shared runtime API.
- OpenCode and Claude Code command flows can launch the new goal setup UI from the setup-goal workflow.
- The implementation includes focused tests for the skill text behavior, data model parsing/serialization, UI state transitions, and server response shape.
- The implementation includes a manual browser verification pass for the bundled interview and facts acceptance flows.
