---
id: D1
trackerStatus:
  type: task
title: '[[D1]]: Exit-code contract (CLI vs hook shim)'
status: unstarted
priority: critical
tags:
- plannotator
- semantics
- contract
- exit-codes
progress: 0
parents:
- "[[PHASE-0]]"
---

## Description

Define and freeze the exit-code contract for all surfaces. This contract phases [[E03]] (state machine), [[E07]] (clear), [[E11]] (JSON output), [[E12]] (hook shims).

## Current draft (from E2E plan — needs confirmation)

| Surface | Approve | Deny | Cancel | Timeout | Illegal-State | Daemon-Down |
|---------|---------|------|--------|---------|---------------|-------------|
| Normal CLI wait/submit | 0 | 1 | 1 | nonzero (defined) | 2 | nonzero |
| Claude hook shim | 0 (JSON `decision.behavior="allow"`) | 0 (JSON `decision.behavior="deny"`) | 0 (JSON) | 0 (JSON) | 0 (JSON error) | 0 (JSON error) |

## Missing rows to define

- daemon unavailable
- daemon crashed while waiting
- malformed daemon response
- port occupied by non-daemon
- protocol/schema mismatch
- `clear --force` while active/idle
- storage failure
- invalid/malformed stdin for hook shim
- SIGINT while waiting (exit 130)

## Acceptance criteria

- Complete exit-code table covering all rows above
- Table reviewed and accepted
- E2E specs reference this contract explicitly
- Implementation matches contract

## Blocks

- [[E03]] (state machine spec)
- [[E07]] (clear spec)
- [[E11]] (JSON output spec)
- [[E12]] (JSON output spec — hook shim)
- [[E14]] (opencode shim)

## Activity Log

- 2026-05-04T08:00:06.000Z: created
