---
id: task_1777436200008_d3
trackerStatus:
  type: task
title: "D3: Daemon crash/recovery contract"
status: to-do
priority: critical
tags: ["plannotator", "semantics", "contract", "crash-recovery"]
progress: 0
parentTaskId: "task_1777436200001_g0"
---

## Description

Separate client death from daemon death. Define what survives a daemon crash and what the CLI must do.

## Cases to define

- Active submitter waiting, daemon receives `SIGKILL`
- Separate `plannotator wait` waiting, daemon receives `SIGKILL`
- Daemon killed after verdict is written but before waiters receive it
- Daemon killed before verdict persistence
- CLI behavior when reconnect/autostart happens vs when it exits with daemon-down

## Options to evaluate

1. **State durable**: restart reconstructs active/resolved state from disk
2. **State ephemeral**: restart enters idle; stale client waits fail with specified code
3. **Partial durability**: specify exactly which states survive (submitted plan, verdict, history, annotations)

## Acceptance criteria

- Contract explicitly states which durability model
- Contract specifies behavior for all five crash cases above
- E2E specs reference this contract explicitly
- Implementation matches contract

## Blocks

- E02 (daemon lifecycle spec)
- E08 (crash recovery spec)

## Activity Log

- 2026-05-04T08:00:08.000Z: created
